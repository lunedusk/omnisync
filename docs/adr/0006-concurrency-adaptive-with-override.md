# Concurrency is adaptive by default with a user-configurable thread limit

Parallel transfers default to an adaptive strategy that respects device and network conditions. The user may override this with an explicit maximum number of concurrent threads (default soft ceiling around 4–6). This keeps mobile safe while allowing power users on desktop to raise the limit.
