# First-run is guided and recovery keys are exportable

On a new device the user is taken through a short guided flow that requires entering or generating a recovery key. An “Export recovery key” action writes an encrypted file the user can store offline. No plaintext recovery material is ever left on disk.
