# Exactly one active Remote in v1

A user may save multiple Remote configurations, but only one may be marked active at a time. This keeps Direction, Conflict Policy and the Local Index model simple and avoids split-brain scenarios. Multiple simultaneous Remotes are deferred to a future version.
