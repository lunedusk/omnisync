# System-file sync uses separate toggles

Instead of a single master switch, the user has independent toggles for:
- Themes & snippets
- Community plugins + their data
- Core Obsidian settings / workspace / bookmarks

This gives precise control over what part of `.obsidian` is synchronised.
