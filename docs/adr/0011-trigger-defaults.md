# Default trigger behaviour

- File-watcher debounce: 30 seconds (minimum; user may raise)
- Periodic / interval sync: off by default
- Sync on Obsidian start: on by default
- Sync on every file save: off by default (the debounced watcher covers this case more efficiently)
